services:
  api:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    ports:
      - "8000:8000"
    env_file: ../.env
    volumes:
      - sentinel-data:/data

volumes:
  sentinel-data: {}
